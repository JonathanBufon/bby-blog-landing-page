<nav class="w-full py-6 bg-slate-900/80 backdrop-blur-md fixed top-0 z-50 border-b border-slate-800">
    <div class="max-w-6xl mx-auto px-4 flex justify-between items-center">
        <a href="{{ path('app_home') }}" class="text-2xl font-bold text-white tracking-tighter hover:text-indigo-400 transition-colors">
            BBY <span class="text-indigo-500">Tech</span>
        </a>
        <div class="flex items-center gap-6">
            <a href="{{ path('app_home') }}" 
               class="text-sm font-medium hover:text-indigo-400 transition-colors {{ app.request.attributes.get('_route') == 'app_home' ? 'text-indigo-400' : '' }}">
                Home
            </a>
            <a href="{{ path('app_blog') }}" 
               class="text-sm font-medium hover:text-indigo-400 transition-colors {{ app.request.attributes.get('_route') starts with 'app_blog' ? 'text-indigo-400' : '' }}">
                Blog
            </a>
            <button 
                type="button"
                data-contact-trigger
                data-contact-endpoint="{{ path('app_contact') }}"
                class="text-sm font-medium hover:text-indigo-400 transition-colors focus:outline-none">
                Contato
            </button>
        </div>
    </div>
</nav>
